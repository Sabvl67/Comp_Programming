#include <iostream>
#include <vector>

using namespace std;

int main(){
    int t, b;
    cin >> t >> b;
    vector<int> pack;
    while(b--){
        int ti;
        cin >> ti;
        pack.push_back(pack);

    } 
}